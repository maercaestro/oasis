# OASIS Margin Optimization Environment Variables
# Copy this file to .env and fill in your actual values

# Email Configuration
EMAIL_PASSWORD=your_gmail_app_password_here
EMAIL_SENDER=maercaestro@gmail.com
EMAIL_RECIPIENT=maercaestro@gmail.com

# Optional SMTP overrides (uncomment if needed)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587

# Gmail App Password Setup:
# 1. Enable 2-factor authentication on your Gmail account
# 2. Go to Google Account settings > Security > App passwords
# 3. Generate an app password for this application
# 4. Replace "your_gmail_app_password_here" with the generated password

# Security Note:
# - Never commit the .env file to version control
# - The .env file should be added to .gitignore
# - Use different passwords for different environments (dev/staging/prod)
